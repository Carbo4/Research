"""Components package.

This package contains dataset utilities, model components, training helpers,
and visualization helpers used by the MarketTransformer project. It exposes
modules such as `dataset`, `utils`, `MarketTransformer`, and `trainer`.
"""

